platform :ios, '9.0'

target 'TrendingFire' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod "pop"
  pod "SwiftyJSON"
  pod "Alamofire"
  pod "SVProgressHUD"
end
